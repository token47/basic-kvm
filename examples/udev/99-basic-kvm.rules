# Example udev rules for basic-kvm devices
# Place this file at /etc/udev/rules.d/99-basic-kvm.rules (requires root)

# Allow non-root read/write access to video devices (v4l2)
KERNEL=="video*", SUBSYSTEM=="video4linux", MODE="0666", GROUP="video"

# Allow non-root access to USB-serial devices (ttyUSB*). Adjust ATTRS{idVendor,idProduct}
# if you want to limit to the specific CH9329 device.
KERNEL=="ttyUSB*", MODE="0666", GROUP="dialout"

# If you know the vendor/product for CH9329 you can restrict to it:
# ATTRS{idVendor}=="1a86", ATTRS{idProduct}=="7523", SYMLINK+="basic-kvm-serial", MODE="0666", GROUP="dialout"
